{$HOSTNAME}.duckdns.org {
    tls {
        dns duckdns {$TOKEN}
    }
    #handle_path /vaultwarden* {
    #    reverse_proxy vaultwarden:80
    #}

    handle_path /nginx* {
        reverse_proxy nginx:80
    }
}