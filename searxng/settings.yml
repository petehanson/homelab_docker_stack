use_default_settings: true

server:
  secret_key: "${SEARXNG_SECRET}"
  limiter: false
  image_proxy: true

ui:
  default_locale: "en"
  default_theme: simple
  infinite_scroll: true

search:
  safe_search: 0
  default_lang: "en"
